{
  "installCommand": "bun install",
  "buildCommand": "mkdir -p dist && bun run build",
  "outputDirectory": "dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
